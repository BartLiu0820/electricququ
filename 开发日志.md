# 电子斗蛐蛐 · 开发日志

> 一个让不同 AI 模型同台竞技小游戏的可视化对战平台。
> 本文档记录项目从立项到当前的完整开发历程与每一阶段的工作日志。

---

## 一、项目概览

**目标**：搭建一个"平台"，让 2~4 个 AI 模型在小游戏中互相比拼，支持 0~1 名 AI 裁判，逐步确认推进比赛节奏，并对比拼过程做可视化展示。

**内置游戏**
- 🪨 石头剪子布·博弈版（出拳前先公开喊话博弈，再秘密出拳同时揭晓）
- 🃏 吹牛（扑克）（扣牌声明点数、可说谎，下家选择跟出或"抓"质疑）
- ➕ 自定义游戏（按标准格式录入规则，由 AI 裁判驱动）

**关键设计决策**
| 决策点 | 选择 | 理由 |
|---|---|---|
| 应用形态 | Vite + React + TS 前端 + 极简 Express 后端代理 | 后端转发规避浏览器 CORS，本地一条命令启动 |
| AI 接入协议 | 统一 OpenAI 兼容格式（`baseURL + apiKey + model`） | 一套代码通吃 DeepSeek / Qwen / Kimi / GLM / OpenAI 等 |
| 推进节奏 | 暂停点驱动状态机，每步等用户确认 | 满足"控制比赛节奏"的核心需求 |
| 密钥管理 | 仅存浏览器 localStorage / 服务端 .env，经本机代理转发 | 不上传任何第三方服务器 |

**技术栈**：React 18、zustand、Express、Vite、TypeScript。动效全部用 CSS，未引入动画库。

---

## 二、项目结构

```
电子斗蛐蛐/
├─ server/index.mjs          # Express：/api/chat 转发代理 + /api/presets 读取 .env 预设
├─ src/
│  ├─ types.ts               # Participant / MatchConfig / GameEvent 等核心类型
│  ├─ engine/
│  │  ├─ aiClient.ts         # 调代理、强制 JSON 输出、解析容错、捕获思考过程
│  │  └─ prompts.ts          # 组装 system/user prompt（规则+目标+可见状态+历史）
│  ├─ games/
│  │  ├─ types.ts            # GameDefinition 扩展点接口
│  │  ├─ registry.ts         # 内置游戏注册 + localStorage 自定义游戏
│  │  ├─ rps/                # 石头剪子布：逻辑 + RpsArena 可视化
│  │  ├─ bluff/              # 吹牛：逻辑 + CardTable 可视化（含偷看底牌）
│  │  └─ custom/             # 自定义游戏：裁判驱动通用运行器
│  ├─ components/            # SetupPage / MatchPage / ConfirmBar / ChatLog / GameEditor
│  └─ store/match.ts         # zustand：对局状态机、确认门控、下一步预取
├─ .env                      # 本地密钥配置（已 gitignore，不入库）
├─ .env.example              # 配置模板
├─ 电子斗蛐蛐.md             # 原始需求文档
├─ 问题记录.md               # 测试中发现的问题清单
└─ 开发日志.md               # 本文档
```

---

## 三、工作日志（按阶段）

### 阶段 0 · 立项与脚手架
**对应提交**：`c27a29a first commit`

- 阅读需求文档 [电子斗蛐蛐.md](电子斗蛐蛐.md)，确认基础功能、角色设定、平台玩法。
- 与用户确认两项关键决策：**Web + 轻量后端**、**统一 OpenAI 兼容协议**。
- 搭建脚手架：`package.json`（concurrently 同起前后端）、`tsconfig.json`、`vite.config.ts`（dev 下 `/api` 代理到 3001）、`index.html`、`server/index.mjs`。
- 实现核心引擎：`types.ts`、`aiClient.ts`（调代理 + JSON 解析容错 + 失败重试 ≤3 次 + 测试机器人兜底）、`prompts.ts`、`store/match.ts`（暂停点状态机 + 确认门控 + 自动连续推进）。
- 定义 `GameDefinition` 扩展点接口，实现游戏注册表。
- 实现两个内置游戏（石头剪子布、吹牛）与自定义游戏（裁判驱动通用运行器）。
- 实现页面：设置页（选游戏→玩家数→各家 API 配置→裁判）、对局页（可视化区 + 确认推进条 + 过程实录 + 比分）。
- **验证**：`tsc` + `vite build` 通过；无头浏览器跑通 4 机器人石头剪子布全场、带裁判吹牛对局，确认每步停在确认条、动效与计分正确、控制台与服务端无报错。

### 阶段 1 · UI 细节修复
- **翻牌判定文字被牌面遮挡**：`.card-chip` 原为行内元素，竖向 padding 只绘制不占布局高度，大牌面下沿压到判定文字。改为 `inline-block`，牌面占满真实高度，与判定文字留出间距。
- **模型名输入框越界**：输入框固有最小宽度撑破 grid 列。给 `.field` 加 `min-width: 0`、输入框改 `width: 100%`，右边缘收回卡片内。

### 阶段 2 · 版面放大与"偷看底牌"
**对应反馈**：扑克牌画面偏小、希望平台使用者能偷看牌面、石头剪子布也要充分利用空间。

- **吹牛版面自适应**：玩家面板改 `auto-fit` 网格铺满竞技区，牌面字号 12→16px，2 人各占一半、3~4 人自动换行；牌桌加高，翻牌大牌面放大。
- **石头剪子布版面**：玩家卡等分网格占满宽度，头像 76px、手势 84px，喊话气泡与比分字号同步加大。
- **新增"偷看底牌"**（[CardTable.tsx](src/games/bluff/CardTable.tsx)）：牌桌上「👁️ 偷看底牌 / 🙈 盖回去」按钮，按声明顺序把底牌堆切回每一手，逐行显示"玩家X 声明 N 张「K」+ 真实牌面"，与声明不符的谎牌带红框标出（王为万能牌不计）。纯观众侧功能，不影响 AI 的信息隐藏。

### 阶段 3 · 密钥安全与 .env 接入
**对应提交**：`11e6b78 chore: 添加 .gitignore…`

- **修复 HTTP 500（端口冲突）**：预览/托管工具会注入 `PORT=5173`，而代理用了 `process.env.PORT`，导致 Express 绑到 5173 而非 3001，前端转发 `/api/chat` 被拒。改用专属的 `API_PORT`（默认 3001）。这是用户实测 "DeepSeek 行动失败：HTTP 500" 的真实根因。
- **.env 预设接入**（[server/index.mjs](server/index.mjs)）：服务端启动加载 .env，按 `前缀_API_KEY / _BASE_URL / _MODEL` 自动识别分组，`/api/presets` 下发预设（key 以 `env:VAR` 引用，明文不进浏览器）。设置页出现"从 .env 导入"芯片，一键填好配置。
- **密钥不入库**：新建 `.gitignore`（排除 `.env`/`.ENV`/`node_modules`/`dist`），新建 `.env.example` 模板；用 `git rm --cached` 移除误提交的 node_modules/dist，并把误命名的 `.ENV` 还原为 `.env`。确认 `git check-ignore .env` 命中、tracked node_modules 归零。

### 阶段 4 · 批量粘贴密钥
**对应反馈**：不想一个框一个框粘，希望一次粘贴三项信息自动识别填入。

- **解析函数 `parseConfigPaste`**（[SetupPage.tsx](src/components/SetupPage.tsx)）：兼容三行、`key=value`、逗号分隔、中英文标签等格式。识别规则：`http(s)://` → Base URL；`sk-` 开头或 20 位以上字母数字 → API Key；其余 → 模型名；带标签者优先按标签走。
- **批量粘贴 UI**：每个玩家/裁判选"真实模型"后出现「📋 批量粘贴」按钮，文本框粘贴后自动识别填入，下方实时提示识别到的字段。
- **过程中修掉两个解析 bug**：
  1. 中文标签前的 `\b`（ASCII 词边界）在中文字符前不成立，导致"模型:"未被消费、回退时把标签词误当模型名 → 去掉 `\b`、支持全角冒号。
  2. "含冒号就跳过"的过滤把 URL 里的 `://` 也算上 → 改为只跳过中文 token 与"纯标签词+冒号"，不误伤 URL。
- **验证**：三种格式在浏览器实测填入正确。

### 阶段 5 · 问题记录 5 项修复
**对应文档**：[问题记录.md](问题记录.md)

| # | 问题 | 处理 |
|---|---|---|
| 1 | 快捷批量粘贴 key | 阶段 4 已完成 |
| 2 | 秘密出拳第一人出拳后问号变成出拳手势 | 见下 |
| 3 | 思考过程长 / 能否文字化展示 | 见下 |
| 4 | 能否预跑、推进只是演绎 | 用户选"预取下一步" |
| 5 | 裁判 temperature 报错 | 见下 |

- **#2 出拳揭幕 bug**（[RpsArena.tsx](src/games/rps/RpsArena.tsx)）：已出拳玩家原显示 🤛（像真出了拳），改为 🔒 锁定态。实测玩家1 出拳后显示 🔒、玩家2 仍 ❔、无揭幕，第二人出拳后统一揭幕。
- **#5 temperature 兼容**（[server/index.mjs](server/index.mjs)）：默认不强制发 `temperature`（Kimi k2.6 等只接受 `temperature=1`），仅显式给值时透传；并加兜底——若因 temperature 报 400 就去掉重试一次。实测 Kimi 返回 200。
- **#3+#4 思考过程展示**（[aiClient.ts](src/engine/aiClient.ts) / [ChatLog.tsx](src/components/ChatLog.tsx) / [prompts.ts](src/engine/prompts.ts)）：捕获 `reasoning_content`，过程实录里以可折叠的「💭 思考过程」暗色卡片展示。思考过程**不进入**对手可见的公开记录、秘密出拳阶段**不展示**。实测 DeepSeek 思考过程正确显示。
- **#3/#4 预取下一步**（[store/match.ts](src/store/match.ts)）：进入"等待确认"时后台立即预算下一个 API 行动者的响应，用户点推进时若已就绪即秒出。实测等预取完成后推进 **51ms** 出结果。预取失败自动回落到正常重试/报错流程。

### 阶段 5.5 · 思考过程 UI 微调
两次随手反馈：
- 思考过程默认**展开**（原 `<details>` 折叠改为常驻）。
- 思考过程并入该玩家本次发言之内、**共用一个名字**，贴在发言之前，用斜体灰字 + 左侧虚线与发言气泡区分，不再单独拎出一个"思考过程"框（[ChatLog.tsx](src/components/ChatLog.tsx)）。

### 阶段 6 · 问题记录 06/13 批次
**对应文档**：[问题记录.md](问题记录.md) 的 06/13 四条。

- **#1+#2 思考总结 + 可展开完整版**（[prompts.ts](src/engine/prompts.ts) / [aiClient.ts](src/engine/aiClient.ts) / [store/match.ts](src/store/match.ts) / [ChatLog.tsx](src/components/ChatLog.tsx)）：要求模型在 JSON 里多输出一个 `thought` 字段（≤300字 心理博弈小结，私密、不发给对手）作为默认展示的精炼总结；完整 `reasoning_content` 收进「展开完整思考」按钮后的独立带边框滚动分区。模型未给 `thought` 时用完整思考截断 300 字兜底。实测 DeepSeek：小结 125 字（自写总结）与完整思维链 185 字内容明显不同，展开/收起正常。
- **#3 开局掷硬币随机先手**（[rps/index.tsx](src/games/rps/index.tsx) / [bluff/index.tsx](src/games/bluff/index.tsx) / [store/match.ts](src/store/match.ts)）：两个游戏的 `setup` 用 Fisher–Yates 随机打乱玩家座次（吹牛同时随机发牌顺序），`startMatch` 播报「🪙 掷硬币决定先后手 —— 本局由「X」优先出手！」。实测连开 6 局先手在玩家间随机变化，播报与首位行动者一致。
- **#4 说话与出手拆开（轻量方案）**（[games/types.ts](src/games/types.ts) / [prompts.ts](src/engine/prompts.ts) 及各游戏）：石头剪子布本就是"先全员喊话→再秘密出拳"，已满足；顺序制的吹牛不强行拆步。改为给"真正出手"的步骤（RPS 出拳、吹牛 lead/respond）打 `decisive` 标记，prompt 在该步追加「关键决断 · 请结合对手发言」区块——单独列出各对手最新公开发言，并明确指示 AI 可推翻原计划、临场改变策略。实测 DeepSeek 喊话称"肯定出石头"实际却出剪刀，正是预期的临场博弈行为，游戏正常完成整轮。

### 阶段 7 · 问题记录 06/13-2 批次
**对应文档**：[问题记录.md](问题记录.md) 的 06/13-2 四条。

- **#1+#2 石头剪子布出拳改为"一步同时出拳"**（[games/types.ts](src/games/types.ts) / [rps/index.tsx](src/games/rps/index.tsx) / [store/match.ts](src/store/match.ts) / [ConfirmBar.tsx](src/components/ConfirmBar.tsx)）：原来每名玩家各占一次推进、且出拳阶段不展示思考；现改为**一次推进、全员并行思考后同时出拳、再统一揭幕**，并在揭幕时展示各自的出拳思考（全员决定完才揭幕，不算剧透）。
  - 引擎层新增 `PendingAction.simultaneous`（同时决策的行动者列表）与 `revealThinking`（保密步骤也展示思考）。`store/match.ts` 重构 `advance`：抽出 `resolveActor`（单人复用），对 `simultaneous` 步骤并行调用各模型、顺序应用、一次性提交；预取也改为对该步全部 API 行动者并行预取。
  - 实测：喊话两步后塌缩为单步「全体玩家 · 同时出拳」，一次推进即揭幕；DeepSeek 玩家的出拳思考随揭幕一起出现（带"展开完整思考"）。
- **#3 吹牛牌桌与手牌重复显示**（[bluff/index.tsx](src/games/bluff/index.tsx)）：质疑结算后，底牌不再**立即**并入输家手牌——改为先留在牌桌展示（新增状态 `pendingCollect`），等输家真正开新一轮出牌时才并入（`effHand` 在出牌前的提示与校验中临时合并，`applyAction` 在该玩家 lead 时正式并入并记一条"收走底牌 N 张并入手牌"）。这样牌桌揭幕期间手牌不增加，杜绝同一张牌在牌桌与手牌重复出现。实测：翻牌展示 ♠2♣2♥2 时与各家手牌零重叠，总牌数恒为 54；输家 lead 时才并入。
- **#4 吹牛王牌识别**（[bluff/index.tsx](src/games/bluff/index.tsx)）：新增 `jokerNote`，在 lead/respond 的可见状态里显式声明"你手中有 N 张王（万能牌）/没有王"，修复模型"有王却以为没有"的误判。实测抓取发给 DeepSeek 的真实 prompt，确认含该提示行。

---

## 四、当前状态

- **已提交**：`c27a29a`（首版平台）、`11e6b78`（密钥治理）。
- **工作区未提交改动**（阶段 4~7）：`server/index.mjs`、`src/store/match.ts`、`src/engine/{aiClient,prompts}.ts`、`src/components/{SetupPage,ChatLog,ConfirmBar}.tsx`、`src/games/rps/{index,RpsArena}.tsx`、`src/games/bluff/{index,CardTable}.tsx`、`src/games/types.ts`、`src/styles.css`、`src/types.ts`，以及新增的 `开发日志.md`。
- **质量门**：每次改动均跑 `tsc --noEmit` 零错误，关键流程用无头浏览器实测（含真实 DeepSeek 调用、抓取请求体核对 prompt）。

## 五、后续可做（备忘）
- 自定义游戏（裁判 AI 驱动）目前仅通过类型检查与表单验证，尚未用真实裁判 key 跑通完整一局。
- NOVA 中转 baseURL（`https://once.novai.su`）未带路径，若调用 404 可能需补 `/v1`。
- 预取是"确认前即发起调用"，若某步看完不推进直接退出，该次预取调用会被浪费（隐藏延迟的固有代价）。
